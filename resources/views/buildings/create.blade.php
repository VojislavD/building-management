<x-app-layout>
    <h1 class="text-xl font-bold">{{ __('Add New Building') }}</h1>
    
    {{-- 
    internal_code = text
    
    status = select -> inactive/active
    
    pib = text 
    identicaication number = text

    account number = text
    
    balance_begining = text

    construction year = select
    square = text

    floors = text
    apartments = text
    tenants = text

    elevator = select -> yes/no
    yard = select-> yes/no

    address = text

    city = text
    municipality = text
    postal_code = text

    comment 
    --}}

</x-app-layout>